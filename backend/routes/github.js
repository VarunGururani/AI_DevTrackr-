const express = require('express');
const router = express.Router();

// Placeholder - will be implemented later
router.get('/', (req, res) => {
  res.json({ message: 'GitHub routes' });
});

module.exports = router;
